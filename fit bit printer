# Program: Fit-Bit Printer
# This program takes an integer 'x' from the user
# and prints a sequence from 1 to x with the following rules:
# 1. If the number ends with 1 (like 1, 11, 21...), print "1".
# 2. If the number is divisible by 5, print "fitbit".
# 3. If the number is even (divisible by 2), print "fit".
# 4. Otherwise, print "bit".

# Input
x = int(input("Enter a number: "))

# Processing & Output
for i in range(1, x + 1):
    if i % 10 == 1:         # numbers ending with 1
        print(1)
    elif i % 5 == 0:        # multiples of 5
        print("fitbit")
    elif i % 2 == 0:        # even numbers
        print("fit")
    else:                   # odd numbers not ending with 1
        print("bit")

Input
Enter a number: 10

Output
1
fit
bit
fit
fitbit
fit
bit
fit
bit
fitbit




# Program: Custom FitBit Pattern Printer
# Description: Prints numbers from 1 to x with the following rules:
# 1. If the number ends with 1, print 1
# 2. If the number is divisible by 5, print "fitbit" and toggle a flag
# 3. If the number modulo 2 equals the flag, print "fit"
# 4. Otherwise, print "bit"

# Input from user
x = int(input("Enter x: "))

# Initialize the flag
flag = 0

# Loop through numbers from 1 to x
for i in range(1, x + 1):
    if i % 10 == 1:
        print(1)
    elif i % 5 == 0:
        print("fitbit")
        # Toggle flag
        if flag==1:
            flag = 0
        else:
            flag = 1
    elif i % 2 == flag:
        print("fit")
    else:
        print("bit")

Input:
Enter x: 16
Output:
1
fit
bit
fit
fitbit
bit
fit
bit
fit
fitbit
1
fit
bit
fit
fitbit
bit
